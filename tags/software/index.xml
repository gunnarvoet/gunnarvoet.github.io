<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Software | Gunnar Voet</title><link>https://gunnarvoet.net/tags/software/</link><atom:link href="https://gunnarvoet.net/tags/software/index.xml" rel="self" type="application/rss+xml"/><description>Software</description><generator>Source Themes Academic (https://sourcethemes.com/academic/)</generator><language>en-us</language><copyright>© 2026 Gunnar Voet</copyright><lastBuildDate>Sat, 29 Aug 2026 00:00:00 +0000</lastBuildDate><item><title>Python Environments with uv</title><link>https://gunnarvoet.net/post/uv/</link><pubDate>Sat, 29 Aug 2026 00:00:00 +0000</pubDate><guid>https://gunnarvoet.net/post/uv/</guid><description>&lt;p&gt;I used to run everything Python through conda and later mamba.
These days I use
&lt;a href="https://docs.astral.sh/uv/" target="_blank" rel="noopener"&gt;uv&lt;/a&gt; and have dropped conda entirely.
Here I document the setup I ended up with and have used for more than a year now, written down mostly so I can find it again, but maybe it is useful to someone else moving over from a conda-based workflow.&lt;/p&gt;
&lt;h2 id="installing-uv"&gt;Installing uv&lt;/h2&gt;
&lt;p&gt;uv is a single binary and installs without a Python interpreter of its own:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;curl -LsSf https://astral.sh/uv/install.sh | sh
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;It also manages Python interpreters, so there is no need to install Python separately.
&lt;code&gt;uv python install 3.13&lt;/code&gt; fetches an interpreter, and &lt;code&gt;requires-python&lt;/code&gt; in a project&amp;rsquo;s &lt;code&gt;pyproject.toml&lt;/code&gt; makes uv pick a matching one on its own.&lt;/p&gt;
&lt;h2 id="one-environment-per-project"&gt;One environment per project&lt;/h2&gt;
&lt;p&gt;Everything is organized by project directory.
Each project has its own environment.
A new project gets&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;uv init
uv add numpy xarray matplotlib
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;which writes a &lt;code&gt;pyproject.toml&lt;/code&gt;, creates a &lt;code&gt;.venv&lt;/code&gt; inside the project, and records exact resolved versions in &lt;code&gt;uv.lock&lt;/code&gt;.
On another machine, &lt;code&gt;uv sync&lt;/code&gt; recreates that environment from the lock file.
This is a feature that wasn&amp;rsquo;t very practical with conda: an &lt;code&gt;environment.yml&lt;/code&gt; with loose version specs resolved differently on my laptop than on the office machine, often months apart, and there was no easy way to exactly define the environment.&lt;/p&gt;
&lt;p&gt;Local packages under development are added as editable installs:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;uv add --editable $HOME/Projects/python/gvpy
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Development-only dependencies go into a group instead of the main dependency list:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;uv add --dev pytest ruff ipykernel
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;I don&amp;rsquo;t need to activate environments anymore.
&lt;code&gt;uv run pytest&lt;/code&gt; or &lt;code&gt;uv run python script.py&lt;/code&gt; runs the command in the project&amp;rsquo;s environment, and &lt;code&gt;uv sync&lt;/code&gt; is implicit, so the environment is up to date before the command starts.&lt;/p&gt;
&lt;h2 id="jupyter"&gt;Jupyter&lt;/h2&gt;
&lt;p&gt;This is the piece that took a bit to sort out, since it replaces what &lt;code&gt;nb_conda_kernels&lt;/code&gt; used to do for me.
The
&lt;a href="https://docs.astral.sh/uv/guides/integration/jupyter/" target="_blank" rel="noopener"&gt;uv docs&lt;/a&gt; suggest&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;uv run --with jupyter jupyter lab
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;which builds a throwaway environment with the project packages plus Jupyter.
This works, but it reinstalls Jupyter and my notebook extensions per project and gives no shared configuration.&lt;/p&gt;
&lt;p&gt;Instead I install Jupyter once as a uv tool, together with the extensions I want everywhere:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;uv tool install --with-requirements $HOME/Projects/python/uv_jupyter_packages.txt jupyter-core
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The requirements file holds &lt;code&gt;notebook&lt;/code&gt;, &lt;code&gt;jupyter-console&lt;/code&gt;, &lt;code&gt;jupyterlab-vim&lt;/code&gt;, &lt;code&gt;jupyterlab-code-formatter&lt;/code&gt;, &lt;code&gt;jupytext&lt;/code&gt;, &lt;code&gt;jupyter-book&lt;/code&gt;, &lt;code&gt;ipywidgets&lt;/code&gt;, &lt;code&gt;ipdb&lt;/code&gt;, and my own
&lt;a href="https://gunnarvoet.net/post/watchmagic/"&gt;watchmagic&lt;/a&gt; and
&lt;a href="https://github.com/gunnarvoet/jupyter-theme-gv" target="_blank" rel="noopener"&gt;&lt;code&gt;jupyter-theme-gv&lt;/code&gt;&lt;/a&gt; as local editable installs.
Editing the file and rerunning the command updates the whole set.&lt;/p&gt;
&lt;p&gt;Each project then registers its own kernel pointing at its &lt;code&gt;.venv&lt;/code&gt;.
With &lt;code&gt;ipykernel&lt;/code&gt; in the project&amp;rsquo;s dev dependencies:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;uv run ipython kernel install --user --env VIRTUAL_ENV $(pwd)/.venv --name=myproject
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;--env VIRTUAL_ENV&lt;/code&gt; part matters.
Without it, the kernel starts with the right interpreter but the wrong environment for anything that shells out or inspects &lt;code&gt;sys.prefix&lt;/code&gt;.
Kernels registered this way show up in every Jupyter session started from the tool install, from any directory, which is the behavior I wanted from &lt;code&gt;nb_conda_kernels&lt;/code&gt; in the first place.&lt;/p&gt;
&lt;p&gt;List and clean up kernels:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;uvx --from jupyter-core jupyter kernelspec list
uvx --from jupyter-core jupyter kernelspec remove myproject
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;I have a few shell aliases for starting Jupyter so I don&amp;rsquo;t have to type the &lt;code&gt;uvx --from&lt;/code&gt; prefix:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;alias jup=&amp;quot;uvx --from jupyter-core jupyter notebook&amp;quot;
alias jupl=&amp;quot;uvx --from jupyter-core jupyter lab&amp;quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id="command-line-tools"&gt;Command line tools&lt;/h2&gt;
&lt;p&gt;Anything I want on &lt;code&gt;$PATH&lt;/code&gt; without belonging to a project gets &lt;code&gt;uv tool install&lt;/code&gt;, which puts it in its own isolated environment with only its entry points exposed:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;uv tool install ruff
uv tool install pdoc
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;For something I run once and don&amp;rsquo;t want installed at all, &lt;code&gt;uvx&lt;/code&gt; fetches and runs it in a temporary environment:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;uvx ruff check .
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id="scripts"&gt;Scripts&lt;/h2&gt;
&lt;p&gt;Standalone analysis scripts can list their dependencies inline, using a
&lt;a href="https://peps.python.org/pep-0723/" target="_blank" rel="noopener"&gt;PEP 723&lt;/a&gt; metadata block. Running&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;uv add --script plot_moorings.py numpy matplotlib xarray
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;adds a comment block at the top of the file listing the requirements.
Adding the shebang&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-python"&gt;#!/usr/bin/env -S uv run
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;and making the file executable means the script builds its own environment on first run and then just works, with no setup instructions to pass along.&lt;/p&gt;
&lt;h2 id="new-projects"&gt;New projects&lt;/h2&gt;
&lt;p&gt;Since most of my research projects share the same layout, I initiate them from a
&lt;a href="https://copier.readthedocs.io/" target="_blank" rel="noopener"&gt;copier&lt;/a&gt;
&lt;a href="https://github.com/gunnarvoet/copier-research-project" target="_blank" rel="noopener"&gt;template&lt;/a&gt; that runs &lt;code&gt;uv sync&lt;/code&gt; and registers the Jupyter kernel as part of project creation, so a new project is ready to open in Jupyter right after&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;copier copy gh:gunnarvoet/copier-research-project my_project
&lt;/code&gt;&lt;/pre&gt;
&lt;!-- ## Where uv does not fit --&gt;
&lt;!-- uv installs from PyPI, so packages that need non-Python libraries can still be awkward. --&gt;
&lt;!-- The scientific packages I rely on daily, `numpy`, `netcdf4`, `matplotlib`, `xarray`, `gsw`, are all on PyPI and install without trouble. --&gt;
&lt;!-- For a project where the compiled dependencies are more tricky, [pixi](https://pixi.sh/) can install conda packages with a similar lock file workflow. --&gt;
&lt;!-- I have not needed this yet. --&gt;
&lt;h2 id="speed"&gt;Speed!&lt;/h2&gt;
&lt;p&gt;uv is fast.
Rebuilding a project of around thirty packages takes well under a second once they are in uv&amp;rsquo;s global cache.
Worrying about a broken environment is no longer worth the time it takes to rebuild one.&lt;/p&gt;</description></item><item><title>watchmagic</title><link>https://gunnarvoet.net/post/watchmagic/</link><pubDate>Fri, 19 Jun 2020 00:00:00 +0000</pubDate><guid>https://gunnarvoet.net/post/watchmagic/</guid><description>&lt;p&gt;
&lt;a href="https://jupyter.org/" target="_blank" rel="noopener"&gt;Jupyter notebooks&lt;/a&gt; are a great tool for explorative data analysis. However, they tend to get a little messy as a project grows in size and I have gotten into the habit of moving most of my code into external packages which I then import into a notebook. This way, I can still keep figures and notes in the notebook while keeping the analysis code modular and being able to edit it in an external editor. Using IPython&amp;rsquo;s &lt;code&gt;%autoreload&lt;/code&gt; magic, the external code can automatically be udpated inside the notebook every time a cell is re-evaluated. Awesome! The only slightly annoying part of this workflow is having to jump back and forth between editor and notebook every time the code has been changed, just to re-evaluate a cell.&lt;/p&gt;
&lt;p&gt;Meet
&lt;a href="https://github.com/gunnarvoet/watchmagic" target="_blank" rel="noopener"&gt;watchmagic&lt;/a&gt;. It adds the &lt;code&gt;%%watch&lt;/code&gt; magic command to IPython. If added to the top of a cell, &lt;code&gt;%%watch&lt;/code&gt; will observe a given directory and re-evaluate the cell on file changes. This way, I only have to save my progress in the editor and can watch the notebook update by itself whenever I change a file. Under the hood,
&lt;a href="https://github.com/gunnarvoet/watchmagic" target="_blank" rel="noopener"&gt;watchmagic&lt;/a&gt; leverages the
&lt;a href="https://pythonhosted.org/watchdog/" target="_blank" rel="noopener"&gt;watchdog&lt;/a&gt; package.&lt;/p&gt;
&lt;p&gt;Installation of
&lt;a href="https://github.com/gunnarvoet/watchmagic" target="_blank" rel="noopener"&gt;watchmagic&lt;/a&gt; is as simple as&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;pip install watchmagic
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A very basic example of displaying a plot in the notebook with analysis code development in the external file &lt;code&gt;science_plots.py&lt;/code&gt; may look like this:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-python"&gt;# import external package with analysis code
import science_plots
# load autoreload magic
%load_ext autoreload
# set autoreload to act on all imports
%autoreload 2
# load watch magic
%load_ext watchmagic
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class="language-python"&gt;%%watch --patterns *.py
# call external code
science_plots.overview_plot()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;After editing &lt;code&gt;science_plots.py&lt;/code&gt; and saving it, the plot will be updated:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;watch: ./science_plots.py has been modified
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src="./Untitled_1_1.png" alt="png" loading="lazy" decoding="async"&gt;
&lt;/p&gt;
&lt;p&gt;By default, &lt;code&gt;%%watch&lt;/code&gt; will look for changes in the current directory. A different directory can be supplied via the &lt;code&gt;--path&lt;/code&gt; command line option. In the example above the &lt;code&gt;--patterns&lt;/code&gt; command line option is used to only look for changes in &lt;code&gt;*.py&lt;/code&gt; files. The following command line options are available:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-shell"&gt;-p PATH, --path PATH provides the PATH to watch for file changes. If
not provided, the current directory will be
watched.
-r, --recursive include subdirectories recursively when
watching for file changes. This is the default
behaviour.
-nr, --non-recursive do not include subdirectories when watching for
file changes.
--patterns &amp;lt;[PATTERNS [PATTERNS ...]]&amp;gt;
look for files with these PATTERNS.
--ignore &amp;lt;[IGNORE_PATTERNS [IGNORE_PATTERNS ...]]&amp;gt;
ignore files containing IGNORE_PATTERNS when
watching for file changes.
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Defaults for a few parameters can be set in the
&lt;a href="https://ipython.readthedocs.io/en/stable/config/intro.html#setting-configurable-options" target="_blank" rel="noopener"&gt;.ipython_config.py&lt;/a&gt; file by adding any of the following lines:&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-python"&gt;#---------------------------------------------------------------
# WatchMagics configuration
#---------------------------------------------------------------
c.WatchMagics.default_patterns = ['*.py']
c.WatchMagics.default_ignore_patterns = ['*.ipynb']
c.WatchMagics.default_case_sensitive = False
c.WatchMagics.default_ignore_directories = True
&lt;/code&gt;&lt;/pre&gt;
&lt;!--
&lt;video controls &gt;
&lt;source src="watchmagic_example.mp4" type="video/mp4"&gt;
&lt;/video&gt; --&gt;</description></item></channel></rss>